#ifndef ARTICULADOS_H
#define ARTICULADOS_H

#include <vector>
#include <queue>

std::vector<int> articulados(std::vector<std::vector<std::pair<int, std::pair<int, int>>>>& adj, int qtdVertices);

#endif